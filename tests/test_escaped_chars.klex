// Test for escaped character patterns
use regex::Regex;
use std::collections::HashMap;

%%
// Test new escaped character patterns (must come before whitespace)
\+ -> PlusEscaped
\* -> MultiplyEscaped
\n -> NewlineEscaped
\t -> TabEscaped

// Test whitespace for separation (space only, not tab)
' ' -> Space
%%
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_escaped_patterns() {
        let input = "+*\n\t";
        let mut lexer = Lexer::from_str(input);
        
        // Debug: print what tokens we get
        let mut tokens = Vec::new();
        while let Some(token) = lexer.next_token() {
            println!("Token: kind={:?}, value='{}'", token.kind, token.text.replace("\n", "\\n").replace("\t", "\\t"));
            tokens.push(token);
        }
        
        // Find specific tokens
        assert!(tokens.iter().any(|t| t.kind == TokenKind::PlusEscaped && t.text == "+"));
        assert!(tokens.iter().any(|t| t.kind == TokenKind::MultiplyEscaped && t.text == "*"));
        assert!(tokens.iter().any(|t| t.kind == TokenKind::NewlineEscaped && t.text == "\n"));
        assert!(tokens.iter().any(|t| t.kind == TokenKind::TabEscaped && t.text == "\t"));
    }
}